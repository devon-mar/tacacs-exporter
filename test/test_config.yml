modules:
  success:
    username: integration
    password: password
    secret: tac_plus_key
  reject:
    username: reject
    password: password
    secret: tac_plus_key
  successenv:
    username: integration
    password: ${TEST_PASSWORD}
    secret: ${TEST_SECRET}
